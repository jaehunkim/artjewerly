# Database
POSTGRES_PASSWORD=changeme

# Admin
ADMIN_PASSWORD=changeme

# CORS (comma-separated origins)
ALLOWED_ORIGINS=http://146.56.113.225

# Frontend API URL (public-facing, used at build time)
# No trailing /api — the code adds /api/* paths automatically
NEXT_PUBLIC_API_URL=http://146.56.113.225

# Cloudflare R2 (optional - images served locally for now)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_KEY=
R2_BUCKET_NAME=heeang-images
R2_PUBLIC_URL=

# Stripe (optional - leave empty to skip payment)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Toss Payments (optional - leave empty to skip payment)
TOSS_SECRET_KEY=
